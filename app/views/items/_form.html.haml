= semantic_form_for @item do |f|
  = f.inputs do
    = f.input :name, :as => :string
    = f.input :value
    = f.input :item_type, :as => :radio, :collection => ["In", "Out"]
    #dp1
      = f.input :next_date, :as => :string
  = f.actions 
